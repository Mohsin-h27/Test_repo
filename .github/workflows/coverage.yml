name: Pytest Coverage Report

on:
  pull_request:
    branches:
      - main  # Adjust to your base branch if needed

jobs:
  run-tests:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: '3.8'  # Adjust as needed

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install pytest pytest-cov coverage

    - name: Run tests for 'read' marker and generate coverage report
      run: |
        pytest --cov=app -k "read" --cov-report=xml --maxfail=1

    - name: Run tests for 'write' marker and generate coverage report
      run: |
        pytest --cov=app -k "write" --cov-report=xml --maxfail=1

    - name: Merge coverage reports for 'read' and 'write' markers
      run: |
        # Install coverage tool to combine the reports
        pip install coverage
        # Combine the coverage reports from both tests
        coverage combine coverage.xml
        coverage html -d ./coverage_html  # Optionally generate HTML for review
        # Create a merged coverage report
        coverage report --show-missing > merged_coverage_report.txt

    - name: Get PR details
      id: pr_details
      run: |
        echo "Fetching PR details..."
        PR_COMMENT_BODY=$(cat merged_coverage_report.txt)
        echo "::set-output name=pr_comment::${PR_COMMENT_BODY}"

    - name: Comment on PR with coverage results
      uses: peter-evans/comment@v3  # Updated to v3
      with:
        issue-number: ${{ github.event.pull_request.number }}
        body: |
          ## Coverage Report for `read` and `write` Markers

          \`\`\`
          ${{ steps.pr_details.outputs.pr_comment }}
          \`\`\`
